defmodule Parser do
  # import NimbleParsec

  # digits = ascii_string([?0..?9], min: 1)
  # number_integer = digits
  # number_float = digits |> string(".") |> concat(digits)

  # defparsec(:expression, number_float)
end
